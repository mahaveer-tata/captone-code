# MySQL datasource
spring.datasource.url=jdbc:mysql://localhost:3306/AIHistory?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Mahaveer@310820
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
# Important: Do NOT update or create tables (same as PostgreSQL)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Import additional secret properties if needed
spring.config.import=classpath:secret.properties

# Server settings
server.port=8080
server.address=0.0.0.0

# API docs path
springdoc.api-docs.path=/v3/api-docs
